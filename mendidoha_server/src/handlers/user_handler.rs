// src/handlers/user_handler.rs
use crate::db::models::User;
use crate::db::schema::users::dsl::*;

pub fn handle_user_request() {
    // Use the User struct and schema here
    println!("Handling user request!");
}
